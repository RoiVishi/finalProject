# Pinned (17.8): the canonical run (registry v4, numbers.json) was produced under
# these exact versions — NFR-REPRO-1 covers the environment, not just the seed.
pandas==3.0.2
numpy==2.4.4
scikit-learn==1.8.0
matplotlib==3.10.9
joblib==1.5.3
tabulate==0.10.0
xgboost==3.2.0
shap==0.51.0
