# Pinned: the registry artifacts (joblib pickles) were trained under these exact
# versions — a silent major bump can break unpickling or change predictions.
# Regenerate the registry (train_compare.py) before upgrading, then re-pin.
fastapi==0.140.0
uvicorn[standard]>=0.29
scikit-learn==1.8.0
pandas==3.0.2
numpy==2.4.4
joblib==1.5.3
xgboost==3.2.0
shap==0.51.0
pydantic==2.13.3
pytest>=8.0
httpx>=0.27
